# Replace OpenAI API keys in git history (run: git filter-repo --replace-text .git-filter-repo-replacements)
# Matches sk-proj-xxx keys and replaces with empty string
regex:sk-proj-[a-zA-Z0-9_-]{50,}==>
